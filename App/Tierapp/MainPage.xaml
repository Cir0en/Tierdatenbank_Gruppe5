<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Tierapp.MainPage">

    <ScrollView>
        <VerticalStackLayout
            Padding="20"
            Spacing="12">
            <Label
                Text="Dashboard"
                Style="{StaticResource Headline}"
                SemanticProperties.HeadingLevel="Level1" />

            <HorizontalStackLayout Spacing="8">
                <Label x:Name="StatusLabel" Text="Status: nicht verbunden" VerticalOptions="Center" />
                <Button x:Name="RefreshBtn" Text="Refresh" Clicked="OnRefreshClicked" />
            </HorizontalStackLayout>

            <CollectionView x:Name="AnimalsCollectionView" SelectionMode="None">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Frame Margin="0,4" Padding="8" BorderColor="#DDD">
                            <VerticalStackLayout>
                                <Label Text="{Binding Name}" FontAttributes="Bold" />
                                <Label Text="{Binding FindDate}" FontSize="12" />
                                <Label Text="{Binding Status}" FontSize="12" />
                            </VerticalStackLayout>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

        
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>



